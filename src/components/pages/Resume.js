import React from 'react'
import { TabTitle } from '../../utils/helpers';

const Resume = () => {
    TabTitle('Resume')
    return (
        <div>
            <h1>Resume</h1>
        </div>
    )
}

export default Resume;