import React from 'react'
import { TabTitle } from '../../utils/helpers';

const Portfolio = () => {
    TabTitle('Portfolio');
    return (
        <div>
            <h1>Portfolio</h1>
        </div>
    )
}

export default Portfolio;